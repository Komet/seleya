{% extends 'SeleyaBundle:Admin:Faculty/base.html.twig' %}
{% block headline %}{% trans %}Fachbereich bearbeiten{% endtrans %}{% endblock %}

{% block content_content %}

<form action="{{ path('admin_faculty_update', { 'id': id }) }}" method="post" {{ form_enctype(form) }} class="form-horizontal">
	{{ form_errors(form) }}
	{{ form_widget(form) }}
	<div class="form-actions">
    	<input class="btn btn-primary" type="submit" value="{% trans %}Fachbereich aktualisieren{% endtrans %}" />
		<a href="{{ path('admin_faculty') }}" class="btn">{% trans %}Abbrechen{% endtrans %}</a>
    	<a href="{{ path('admin_faculty_delete', { 'id': id }) }}" class="btn btn-danger left-space-medium"><i class="icon-trash icon-white"></i> {% trans %}Fachbereich löschen{% endtrans %}</a>
	</div>
</form>

{% endblock %}
